// When user logs in : 

- need to check if any items are in local storage ->
if so add mutation to user.cartItems to input those items then delete local storage.

// When cart is rendered:
 need to first check if there is a user, if so render the graph ql cart items, 
 if not render the items in local 
 
 // When item is added: 
 need to check if user is logged in. If so mutate the backend, if not add to local storage. 